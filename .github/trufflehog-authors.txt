# email → GitHub login, consulted only when neither the commit itself nor the
# pull request it arrived with names an account. That happens when a work
# address is not attached to the account that owns it.
#
# Temporary by intent: an entry stops being needed the moment its owner adds
# the address under Settings → Emails, and the file can later give way to a
# real identity lookup.
#
# Format: <email>  <login>

alexey.panfilov@constructor.tech       dzarlax
grigoriy.gogin@constructor.tech        Gregory91G
konstantin.tursunov@constructor.tech   ktursunov
roman.mitasov@constructor.tech         mitasovr

# mikhail.balayan@acronis.com — four commits carry this address and none of
# them, nor their pull requests, resolve to an account; no matching account
# was found. Findings from those commits stay unassigned and name the address.
